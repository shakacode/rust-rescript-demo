include %css(
  let h = css`
    display: flex;
    flex-flow: row nowrap;
    font-family: ${Font.title};
  `

  let h1 = css`
    font-size: 30px;
    font-weight: ${Font.bold};
  `

  let h2 = css`
    font-size: 24px;
    font-weight: ${Font.normal};
  `
)
